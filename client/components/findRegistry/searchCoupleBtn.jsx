import React from "react";

const SearchCoupleBtn = ({ checkCookies }) => {
  //   return checkCookies ? <button>Find registry</button> : <h1> hi!!!</h1>;
  return <button>Find registry</button>;
};

export default SearchCoupleBtn;
